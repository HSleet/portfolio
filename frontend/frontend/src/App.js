import React from 'react';
import { BrowserRouter as Router } from 'react-router-dom';
import Base from './Base';

const App = () => {
  return (
    <Router>
      <Base />
    </Router>
  );
};

export default App;